Linux and docker INFO
mv creates the new directory | renames an existing directory n can also be used for 
#!/bin/bash here # is called the shabang operator helps in including the interpreter of our choice in our shell file like over here we have used bash instead of shell or any other
after @ is the host machine name while before it is the user name
Absolute vs relative path mv cp ->                                                                                                             from the root to desired dirr while relative path is relative to where you are currently at so yes depends on requirement but yes file name is required to be present on the first argument or src
$ means non root user while # means root user
we can't use $0 within an expression since it refers to the bash file 
in order to do sudo you should ensure that the current user is mentioned in the user group of sudoers

TODO: also putting $ for user input vars inside of $() makes no sense but look for true bug behind it
.'. go with $1/$2 | awk or $(1/2) | bc
then run file like this ./someDirr or ./some_name.sh arg1 arg2 which isn't clean therefore go with $(1/2)
TODO: optimized 
#!/bin/bash
read -p "Enter first number : " num1 where -p flag means to give prompt to the user
you can also make it more secure for passwords by utilizing silent prompt mechanism
read -sp "Enter password" password                                                                                      silent prompt
TODO: 2 begin with
#!/bin/bash
echo "Enter first number :"
read num1
echo "Enter second number :"
read num2

don't use $ inside of $(()) ensure that you use two set of parenthesis at least while using this 
also conditions are required to be present inside of [] instead of () as scene in the case of c style for loop here in bash which is 

equal                                                                                                                                                           -eq
not equal                                                                                                                                                           -ne
less than                                                                                                                                           -lt
less than equal to                                                                                                                                                                                    -le
greater than                                                                                                                                                                  -gt
greater than equal to                                                                                                                         -ge
Loops
  1. if
    if[$someVar -gt $anotherVar]//also works with && and || 
    then 
      echo 
    elif[$someVar -le $anotherVar]
    then 
      echo 
    else 
      echo
    fi
  2. While 
    read -p "Enter a number" num
    while[$num -le 7]
    do 
      echo "current value $num"
      num=$((num+1))
    done
when we download wsl it brings a Linux kernel along with it but when we go with a docker container having some Linux image then this guest operating system doesn't come with any kernel
TODO: Git 
rebase stash
🐳 Git & Github 
git fetch just downloads the code from the corresponding branch doesn't merge right away
.'. the working branch remains unchanged
but good practice would be to do
git log origin/main 
git diff origin/main 
on the other git pull is dangerous since it straightaway merges code from the branch being pulled into the  
git pull origin branchName = git fetch origin branchName git merge branchName 
to create new branch and checkout to it ->                                                                                          git branch brName else git checkout -b brName 
checking out a branch ->                                                                                    git checkout brName
FIXME: use case ?? how to create nested branches ->                                                                                                                          git branch -b newBr oldBr(main)
FIXME: how to create local branch from remote branch as reference ->                                                                          git branch -b brName origin/remoteBr                                                                        
to delete branch n delete even if it isn't merged properly ->                                                                                       git branch -d brName else git branch -D brName 
to delete remote branch ->                                                                                                              git push origin --delete brName
Note that remote n local can have their own branches they may not be knowing 
when we perform git clone git downloads remote tracking references hit git branch -r output origin/main origin/feature etc 
these are remote-tracking branches not local branches. 
FIXME: how would you see all remote and local branches ? ->                                                                 git branch -a
FIXME: how would you create a branch                                                                                                gir branch brName
.'. how to upload a local branch to remote ->                                                                                   git checkout localBranchName then git push -u origin brName 
we can get a cleaner git history from merge using                                                                                                               git rebase
when we make changes to a file and directly hit git pull then we are presented with the message commit or stash local changes first
stash hides away the local changes temporarily making the working directory clean to welcome the git pull contents later you can do git stash pop
FIXME: git stash commands
git stash 
git stash list 
git stash pop 
git stash apply
🐋Docker 🐬
to add current user to corresponding ->                                                                                sudo usermod -aG docker/jenkins currentUser FIXME: Also do sudo newgrp docker/jenkins or you can logout and then log back in(🤠)to refresh the current session so that it doesn't use old group settings
FIXME: Note for cmds with docker we always right that first followed by cmd ex docker network/volume create netName
docker rmi 
we can give tags to images alone and not to containers and when not given docker assumes it to be  this tag goes to DockerHub when we push it 
when container DOESN'T run in the detached mode then its lifecycle gets hardly coupled with the terminal session
sudo usermod -aG 
FROM someImg
WORKDIR /containerDir
COPY someFileOrDirr . <- meaning from current directory itself which is what's usually seen
CMD javac FileName.java
RUN ["java","FileName"]
docker run -itd\
  --name someName \ 
  --network someNet \ 
  --v volName:/data \ (TODO: May be single hyphen ?)
  imageName sleep infinity
  -itd 
    i stands for interactive(i.e. stdin open)
    d stands for detached mode 
    t means to allocate a pseudo-TTY terminal
  --v volName:/data attaches the volName to the container on its data directory   
  last statement since we want the volume to persist even if container has stopped running ? 
then how do we enter the container ->                                                                                 docker exec -it contName bash|shell
the working directory(/app) belongs to the container and not to the local directories that you would need to create one for convenience
Considering docker arch when we hit a command on the terminal(lets say docker run), we are using the docker cli tool packages that command within an api request and sends it to the docker daemon(dockerd)
a quick note the detached mode of 
docker network create my-custom-network 
docker run -d --network my-custom-network containerB
1st | 2nd April 2026 
tenants cast complaints, requests and suggestions against their owners, 
Tenants, owners,  Building < Floor < Flat < Room < Tenant 
operator in cpp used alongside input stream or cin -> 																					extraction 
one with cout or << is called -> 																															insertion
other name for values stored in data members of the object -> 																					state of an object 
order of arguments considering default values in cpp vs java-> 																			for constructors its always left to right but when it is className(int a=78, int ....,) i.e. providing default values at the param level itself then you should know that the most stable props should be kept earlier while the ones which are been given defaults should be kept at the last or latter potion like this 
className(int prop1,float prop2, string prop3="Hello world"){}//if prop3 was kept earlier with default value then invalid choice
Note java doesn't allow such defaults at the constructor level so either go for telescopic constructors or constructor overloading or otherwise 
provide defaults at the point of declaring the the corresponding property
size of object of empty class is always -> 																	can be 1 byte or 8 bytes
unbounded type inference includes ? with super  
multiple inheritance removes the ability of ->                                                                                      dynamic binding & polymorphism because we cannot even extend anymore 
operator precedence in java -                                                                                                                               Parentheses -> postfix(var++/var--) -> Multiplicative(*,/,%) -> Additive(+,-) -> Relational(<,>,<=,>=) -> Equality(==,!=) -> Logical AND -> Logical OR -> Assignment (=,+=,-=, etc)
correct terminology for methods of an interface                                                                 Abstract Method declaration(name+param list+return type because method singature means method name n the parameter list alone 
default access specifier of an interface's method signature:                                                         public abstract and if we try to reduce the visiblity from public-> protected or private then throws exception cannot reduce visibility of the inherited method from the interface  
protected data members ->                                                                                                                                       accessible within class(common in all 3), accessible in un-extended classes as long as in the same package so of course extended classes can access it BUT note that if we are accessing this protected method from within (method) of a child class then it won't be able to access it via instantiatoin of the parent class. You can use super but parentObj = new Parent(); parentObj.protectedMethod() will throw compile time error.Of course for classes which don't even belong to the same package and have not inherited either then you can't access except for public data members. 
hashCode and equals are required to be overridden together ->                                                                                                                                            because equals checks hashcode itself for the participating references
//todo what kinds of declared vars or methods can't be used or called inside of a constructor and more types of such questions.
order of execution of constructors in multi level inheritance is parent->child and in cpp where we destructors 2 the order of exec for ~constructors is reverse child->parent
//write to file object by object since previously had data which was not generic 
so if kept generic then casting will be needed so may be some output statement writing getRole() + employee name + into file 
Furthermore we would be needing the code for reading from file and integrate that with comprable or comparator accordingly
map.keySet() : entrySet() etc
should we keep more number of cases to add Manager, Sales Manager etc or instead have yet another do while for sub cases within 
parent switch case ? 
comparable alone sufficient or would we also be needing comparator ? 

//todo super to be kept as the topmost statement within child constructor and destructor can be kept anywhere ? What was the other 
entity ? 

//here onwards new stuff for lab exam 
generic node class takes ____ in the constructor cause that is what is the type of data it holds :                        T
of course the next and prev will be the type of the class 

Stream aggregate funcs implement following respective Functional interfaces 
filter : Predicate(test: T->Boolean)
map : Function(apply: T,T)
forEach : consumer(accept: T)
reduce : supplier ?(get) 
Prior: String Buffer vs Latter: String Builder
1. P is older(java 1.0) while L was introduced @ 1.5
2.Older is definitely slow but dependable since thread safe while L is fast not thread safe
3. P being thread safe therefore synchronized while L isn't
4. P sync .'. used in multi-threaded environment while L can be used in single threaded environment alone

avl tree questions balance factor type of rotations - L, R, LL, LR

ObjectInputStream takes a FileInputStream object in its constructor
ObjectOutputStream takes a FileOutputStream object in its constructor
Two Sum II - Two pointers technique
3Sum - Classic array problem with sorting
Container With Most Water - Two pointers
Longest Substring Without Repeating Characters - Sliding window
Product of Array Except Self - Array manipulation
Group Anagrams - HashMap and string manipulation
Longest Palindromic Substring - Dynamic programming/expand around center
Rotate Image - Matrix manipulation (90-degree rotation)
Set Matrix Zeroes - In-place array modification
Subarray Sum Equals K - HashMap + prefix sum

Stack & Queue

Valid Parentheses (Easy but important foundation)
Min Stack - Stack with O(1) min operation
Daily Temperatures - Monotonic stack
Evaluate Reverse Polish Notation - Stack basics
Next Greater Element II - Circular array + monotonic stack
Simplify Path - Stack for file paths
Decode String - Nested stack problem
Asteroid Collision - Stack simulation
Remove K Digits - Greedy + monotonic stack
Largest Rectangle in Histogram - Classic monotonic stack

____TODO: Java topics
Wild cards
Overloading mcq
Multi Threading 
link  
____TODO: logic bugs dsa
graph implementation where we were supposed to initialize an empty array list for the nodes then type ->                                                                                                                                you added it instead of putting it in as its a map not a list
____Garbage Collection
Gc can consider an object multiple times but finalyze() can be called only once for that object
Marking it finalyzed from then onwards and later gc calls will destroy the memory contents without any warning 

For an object to be eligible for gc it needs to loose its reference so that its original internal state existing some where in the auxiliary space is not reachable to the object anymore. Then System.gc() will be called for that object but right before destroying its dangling? Memory contents from the ram the finalyzed method will be called
To give one last opportunity to the user in case you still want to work with thay object or not because internal state(via param or default constructor) still exists in the memory and therefore we can use object resurrection to reclaim the reference of the nullified object for using it again. BUT here comes the reason why 
Finalyzed marked objects get deleated by Gc without any warning and if certain expensive resources were present as properties to that object then db connections, files then they may have already been released 
____CPP
data type provided by cpp and absent in C                                                                                                                                                                                                       bool is provided by cpp was absent in C
_____
_____HTML
padding is ->                                                                                                                                                                       the space between the edge or the border and the content
inner children of ul, ol and tr ->                                                                                                                          ul->li ol->li
dl, dt or definition list or definition 
_____
Linked Lists

Reverse Linked List II - Partial reversal
Add Two Numbers - Linked list arithmetic
Remove Nth Node From End - Two pointers
Swap Nodes in Pairs - Pointer manipulation
Odd Even Linked List - Rearranging nodes
Sort List - Merge sort on linked list
Reorder List - Multiple techniques combined
Copy List with Random Pointer - HashMap cloning

Trees & Binary Search Trees

Binary Tree Level Order Traversal - BFS
Binary Tree Zigzag Level Order Traversal - BFS variant
Validate Binary Search Tree - BST property
Kth Smallest Element in BST - Inorder traversal
Lowest Common Ancestor of Binary Tree - Recursion
Binary Tree Right Side View - BFS/DFS
Path Sum II - DFS + backtracking
Construct Binary Tree from Preorder and Inorder - Tree construction
Flatten Binary Tree to Linked List - Morris traversal
Binary Tree Maximum Path Sum (harder medium) - Recursion

Graphs & BFS/DFS

Number of Islands - DFS/BFS on grid
Clone Graph - Graph traversal + cloning
Course Schedule - Topological sort (cycle detection)
Course Schedule II - Topological sort (order finding)
Pacific Atlantic Water Flow - Multi-source BFS/DFS
Word Ladder - BFS shortest path
Surrounded Regions - DFS/BFS on borders
Rotting Oranges - Multi-source BFS
Network Delay Time - Dijkstra's algorithm

Dynamic Programming

Coin Change - Classic DP
Longest Increasing Subsequence - DP/Binary search
House Robber II - Circular DP
Decode Ways - 1D DP
Unique Paths - 2D DP (grid)
Jump Game - Greedy/DP
Word Break - DP + string
Partition Equal Subset Sum - 0/1 knapsack
Longest Common Subsequence - 2D DP
Maximum Product Subarray - DP variant

Backtracking

Permutations - Classic backtracking
Subsets - Backtracking combinations
Combination Sum - Backtracking with duplicates allowed
Generate Parentheses - String backtracking
Letter Combinations of Phone Number - Backtracking
Palindrome Partitioning - Backtracking + DP

Binary Search

Search in Rotated Sorted Array - Modified binary search
Find Peak Element - Binary search on unsorted
Search a 2D Matrix - 2D binary search
Find First and Last Position - Binary search bounds
Koko Eating Bananas - Binary search on answer
Capacity to Ship Packages - Binary search on answer

Heaps/Priority Queue

Top K Frequent Elements - Heap/bucket sort
Kth Largest Element - Quickselect/heap
Find Median from Data Stream - Two heaps
Task Scheduler - Greedy + heap
Reorganize String - Heap greedy

Two Pointers / Sliding Window

Minimum Window Substring - Sliding window (harder)
Find All Anagrams in String - Sliding window
Longest Repeating Character Replacement - Sliding window
Permutation in String - Sliding window

Must-Do Top 20 for Stack Focus
If you want to focus specifically on Stack problems, here are the essentials:

Valid Parentheses ⭐
Min Stack ⭐
Daily Temperatures ⭐⭐
Next Greater Element I & II ⭐⭐
Evaluate Reverse Polish Notation ⭐
Decode String ⭐⭐
Asteroid Collision ⭐⭐
Simplify Path ⭐
Remove K Digits ⭐⭐
Largest Rectangle in Histogram ⭐⭐⭐
Maximal Rectangle (uses histogram logic) ⭐⭐⭐
Validate Stack Sequences ⭐
Basic Calculator II ⭐⭐
Remove All Adjacent Duplicates II ⭐⭐
Online Stock Span ⭐⭐
